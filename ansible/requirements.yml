---
# Install with:  ansible-galaxy collection install -r requirements.yml
collections:
  - name: kubernetes.core
    version: ">=3.0.0"
  # Decrypts the SOPS-encrypted secrets file at deploy time (community.sops.sops lookup).
  - name: community.sops
    version: ">=1.6.0"
