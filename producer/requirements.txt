# Runtime dependencies, pinned for reproducible builds. The `[avro]` extra pulls the
# Schema Registry Avro serializer + its HTTP/fastavro deps. The Yahoo poller uses the
# stdlib (urllib), so there's no HTTP client dependency. A full pip-compile lockfile is
# the production follow-up; exact `==` pins are the PoC baseline.
confluent-kafka[avro]==2.5.3
fastavro==1.9.7
pydantic==2.9.2
pydantic-settings==2.5.2
